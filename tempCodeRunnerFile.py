ames']) > 0 and len(res['Distances']) > 0 and res['Distances'][0][0] >= 0.7:
        #     for name, dist in zip(res['Names'], res['Distances']):
        #         if dist[0] > conf.threshold_distance:
        #             print(name[0], dist[0])